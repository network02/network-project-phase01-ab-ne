import java.io.IOException;
import java.net.Socket;

public class Request {

    private Socket socket;



    /*
    public boolean isOnline(String IPAddress, int portNumber) throws IOException {

        socket = new Socket(IPAddress,portNumber);

        if (socket.isConnected())
            return true;
        else return false;

    }


    void checkPorts()
    {
        socket = new Socket();
        socket.connect();

    }

    void get(){}

    void post(){}

     */
}
